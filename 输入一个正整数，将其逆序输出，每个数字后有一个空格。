#include<stdio.h>
int main()
{
    int i,n,a;
    scanf("%d",&n);
    for(i=1;i<=10;i++)
    {
        if(n==0)
        continue;
        a=n%10;
        n=n/10;
        printf("%d ",a);
    }
    return 0;
}
