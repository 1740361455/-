int titleToNumber(char * columnTitle){
    int sz=strlen(columnTitle);
    int count=0;
    for(int i=0;i<sz;i++)
    {
        count*=26;
        count+=(columnTitle[i]-64);
    }
    return count;
}
