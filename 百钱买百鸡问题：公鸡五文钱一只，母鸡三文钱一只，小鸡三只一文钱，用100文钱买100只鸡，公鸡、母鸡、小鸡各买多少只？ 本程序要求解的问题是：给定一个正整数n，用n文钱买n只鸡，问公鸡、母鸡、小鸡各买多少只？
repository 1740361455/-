#include <stdio.h>
int main()
{
	int n,a=5,b=3,c,i,j,m;
	scanf("%d",&n);
	for(i=0;i<=n;i++)
	{
		for(j=0;j<=n;j++)
		{
			c=3*(n-i*a-j*b);
			if(i+j+c==n)
			printf("%4d%4d%4d\n",i,j,c);
			else
			m++;
		} 
	}
	if((n+1)*(n+1)==m)
	printf("No Answer");
    return 0;
 }
