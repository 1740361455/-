#include<stdio.h>
#include<string.h>
int main() {
	char s[] = "j";
	for (int i = 0; i < strlen(s); i++) {
		int x = 0;
		for (int j = 0; j < strlen(s); j++) {
			if (i == j)
				continue;
			if (s[i] != s[j])
				x++;
		}
		if (x == strlen(s) - 1) {
			printf("%c", s[i]);
			return 0;
		}
	}
	printf(" ");
}
