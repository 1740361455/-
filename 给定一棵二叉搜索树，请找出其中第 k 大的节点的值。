int ergodic(struct TreeNode* root,int* p)
{
    if(root==NULL)
    return ;
    ergodic(root->right,p);
    if((*p)==0)
    return root->val;
    (*p)--;
    ergodic(root->left,p);
}
int kthLargest(struct TreeNode* root, int k){
    int* p;
    *p=k-1;
    ergodic(root,p);
}
