#include <stdio.h>
int main() 
{
	int n,a=1,b=1,c,i;
	scanf("%d",&n);
	if(n==1||n==2)
		printf("%d",a);
	else 
	{
		for(i=1; i<n-1; i++) 
		{
			c=a+b;
			a=b;
			b=c;
			
		}
		printf("%d",c);
	}
	return 0;
}
