#include <stdio.h>
#include <string.h>
int main(){
	char arr[]={"aaabbb"},brr[]={"bbbaaa"};
	int a=0,A;
	A=strlen(arr)-1;
	while(brr!=arr)
	{
		brr[a]=arr[a];
		brr[A]=arr[A];
		a++;
		A--;
	 printf("%s\n",brr);
	}
	return 0;
}
