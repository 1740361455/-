#include <stdio.h>
#include <math.h>
int main()
{
	long long n,m,a,b=0,i,j;
	scanf("%lld%lld",&n,&m);
	for(i=1;i<=n;i++)
	{
		for(j=1;j<=n;j++)
		{
			a=pow(i,2)+pow(j,2);
			if(a%m==0)
			b++;
		}
	}
	printf("%lld",b);
    return 0;
}
