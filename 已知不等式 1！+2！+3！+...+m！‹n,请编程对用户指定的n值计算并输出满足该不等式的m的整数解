#include<stdio.h>
int main()
{
    int n,m=1,i,num;
    long sum;
    scanf("%d",&n);
    while(sum<n)
    {
        sum=0;
        num=1;
        for(i=1;i<=m;i++)
        {
            num*=i;
            sum+=num;
        }
        m++;
    }
    printf("m<=%d",m-2);
    return 0;
}
