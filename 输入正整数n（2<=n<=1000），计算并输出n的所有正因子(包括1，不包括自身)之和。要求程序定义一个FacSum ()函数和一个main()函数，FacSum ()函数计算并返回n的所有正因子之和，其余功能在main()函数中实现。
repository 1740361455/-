#include <stdio.h>
int FacSum(int n)
{
	int i,sum=1;
	for(i=2;i<n;i++)
	{
		if(n%i==0)
		sum+=i;
	}
	return sum;
}
int main()
{
	int n,sum;
	scanf("%d",&n);
	sum=FacSum(n);
	printf("%d",sum);
	return 0;
}
