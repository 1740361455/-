#include <stdio.h>
#include <math.h>
int main()
{
	long long m,n,x=0,y=0;
	scanf("%lld%lld",&m,&n);
	for(m;m<=n;m++)
	{
		if(m%2==0)
		x+=pow(m,2);
		else
		y+=pow(m,3);
	}
	printf("%lld %lld",x,y);
	return 0;
 } 
