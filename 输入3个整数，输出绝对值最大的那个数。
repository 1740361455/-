#include<stdio.h>
#include<math.h>
int main()
{
	int a,b,c,max;
	scanf("%d%d%d",&a,&b,&c);
	if(abs(a)>=abs(b))
	max=a;
	else
	max=b;
	if(abs(max)>=abs(c));
	else
	max=c;
	printf("%d",max);
    return 0;
}
