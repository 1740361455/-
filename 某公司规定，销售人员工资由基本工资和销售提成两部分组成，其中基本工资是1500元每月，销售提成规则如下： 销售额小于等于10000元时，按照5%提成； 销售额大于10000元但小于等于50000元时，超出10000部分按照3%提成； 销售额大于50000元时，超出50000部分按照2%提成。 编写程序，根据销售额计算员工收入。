#include <stdio.h>
int main()
{
	int n;
	float sum;
	scanf("%d",&n);
	if(n<=10000)
	sum=1500+n*0.05;
	else if(n<=50000)
	sum=1500+10000*0.05+(n-10000)*0.03;
	else
	sum=1500+10000*0.05+40000*0.03+(n-50000)*0.02;
	printf("%.2f",sum);
	return 0;
}
