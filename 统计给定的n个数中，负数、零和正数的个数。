#include<stdio.h>
int main()
{
    int n,i,a=0,b=0,c=0,d;
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        scanf("%d",&d);
        if(d<0)
        a++;
        else if(d==0)
        b++;
        else
        c++;
    }
    printf("%d %d %d",a,b,c);
    return 0;
}
