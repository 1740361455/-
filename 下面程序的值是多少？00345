#include <stdio.h>
int main()
{
	int sz,i,arr[]={1,2,3,4,5};
	sz=sizeof(arr)/sizeof(arr[0]);
	short* pa=(short*)arr;
	for(i=0;i<4;i++)
	*(pa+i)=0;
	for(i=0;i<sz;i++)
	printf("%d",arr[i]);
	return 0;
}
