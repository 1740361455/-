#include <stdio.h>
#include <math.h>
int main()
{
	int n,i,num1,sum,num2;
	while(scanf("%d",&n)!=EOF)
	{
		sum=1;
		for(i=1;i<=n;i++)
		{
			scanf("%d",&num1);
			num2=abs(num1);
			if(num2%2==1)
			sum*=num1;
		}
		scanf("\n");
		printf("%d\n",sum);
	}
}
