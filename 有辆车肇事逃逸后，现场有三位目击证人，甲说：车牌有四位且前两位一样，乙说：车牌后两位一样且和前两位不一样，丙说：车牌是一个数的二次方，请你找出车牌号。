#include<stdio.h>
#include<math.h>
int main()
{
	int n;
	for(int i=(int)sqrt(1000);i<=(int)sqrt(9999);i++)
	{
		n=i*i;
		if(n%10==n/10%10&&n/1000==n%1000/100&&n%10!=n/1000)
		{
			printf("%d",n);
			break;
		}
	}
	return 0;
}
