int cuttingRope(int n){
    if(n<=3)
    return n-1;
    int div=n/3;
    int rem=n%3;
    int max=1;
    if(rem==0)
    max=pow(3,div);
    else if(rem==1)
    max=pow(3,div-1)*4;
    else
    max=pow(3,div)*2;
    return max;
}
