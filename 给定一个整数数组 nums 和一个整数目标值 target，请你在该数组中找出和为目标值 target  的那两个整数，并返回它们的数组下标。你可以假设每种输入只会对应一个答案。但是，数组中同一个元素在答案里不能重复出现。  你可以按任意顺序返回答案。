#include<stdio.h>
void twoSum(int* nums, int numsSize, int target)
{
int i,j,a,b=0;
for(i=0;i<numsSize;i++)
{
    a=target-nums[i];
    for(j=0;j<numsSize;j++)
    {
        if(a==nums[j])
        {
            b++;
            break;
        }
    }
    if(b==1)
    break;
}
printf("[%d,%d]",i,j);
}
int main()
{
	int nums[]={9,2,3,4,5,6,7,8,1};
	int numsSize=sizeof(nums)/sizeof(nums[0]);
	int target;
	scanf("%d",&target);
	twoSum(nums,numsSize,target);
	return 0;
}
