#include <stdio.h>
int main()
{
	int n,x;
	while(scanf("%d\n",&n)!=EOF)
	{
		x=0;
		if(n==1)
		printf("%d\n",x);
		else
		{
			while(n!=1)
			{
				if(n%2==0)
				n/=2;
				else
				n=n*3+1;
				x++;
			}
			printf("%d\n",x);
		}
	}
	return 0;
}
