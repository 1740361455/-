#include <stdio.h>
int main()
{
	int n=0;
	char arr;
	while(1)
	{
		scanf("%c",&arr);
		if(arr=='\n')
		break;
		else if(arr>=48&&arr<=57)
		n++;
		else
		continue;
	}
	printf("%d",n);
	return 0;
 } 
