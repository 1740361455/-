#include<stdio.h>
#include<string.h>
int main()
{
    char arr[1001];
    int i,n=0;
    gets(arr);
    for(i=0;arr[i]!='\0';i++)
    if(arr[i]==' '&&arr[i+1]!=' ')n++;
    if(arr[0]!=' ')n++;
    if(arr[i-1]==' ')n--;
    printf("%d",n);
    return 0;
}
